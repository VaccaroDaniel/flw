define("mod_flwvrroom/room",["core/ajax","core/notification"],function(Ajax,Notification){var started=Date.now();var clamp=function(value,min,max){return Math.max(min,Math.min(max,value));};var updateScore=function(root,passinggrade,maxgrade){var score=0;var completed=[];root.querySelectorAll("[data-hotspot].is-complete").forEach(function(button){score+=parseInt(button.getAttribute("data-score"),10)||0;completed.push(button.getAttribute("data-hotspot"));});var answer=root.querySelector("input[type=radio]:checked");if(answer){score+=parseInt(answer.value,10)||0;}score=clamp(score,0,maxgrade);root.querySelector('[data-region="score"]').textContent=score;root.classList.toggle("is-passed",score>=passinggrade);return{score:score,completed:completed};};var init=function(config){var root=document.getElementById(config.rootid);if(!root){return;}var stage=root.querySelector('[data-region="panorama-stage"]');if(stage){var panAmount=function(){return Math.max(240,Math.round(stage.clientWidth*.55));};var pan=function(direction){stage.scrollBy({left:direction*panAmount(),behavior:"smooth"});};root.querySelectorAll('[data-action="pan-left"]').forEach(function(button){button.addEventListener("click",function(){pan(-1);});});root.querySelectorAll('[data-action="pan-right"]').forEach(function(button){button.addEventListener("click",function(){pan(1);});});}root.querySelectorAll("[data-hotspot]").forEach(function(button){button.addEventListener("click",function(){button.classList.add("is-complete");button.setAttribute("aria-pressed","true");updateScore(root,config.passinggrade,config.maxgrade);});});root.querySelectorAll("input[type=radio]").forEach(function(input){input.addEventListener("change",function(){updateScore(root,config.passinggrade,config.maxgrade);});});var save=root.querySelector('[data-action="save-attempt"]');var status=root.querySelector('[data-region="status"]');save.addEventListener("click",function(){var result=updateScore(root,config.passinggrade,config.maxgrade);save.disabled=true;status.textContent="Saving...";Ajax.call([{methodname:"mod_flwvrroom_submit_attempt",args:{cmid:config.cmid,score:result.score,completedobjects:result.completed.join(","),taskcomplete:result.score>=config.passinggrade,durationseconds:Math.round((Date.now()-started)/1000)}}])[0].then(function(response){root.querySelector('[data-region="best-score"]').textContent=Math.max(parseInt(root.querySelector('[data-region="best-score"]').textContent,10)||0,response.score);status.textContent=config.strings.saved+" Score: "+response.score+(response.passed?" / Passed":" / Try again");return response;}).catch(function(error){status.textContent=config.strings.savefailed;Notification.exception(error);}).then(function(){save.disabled=false;});});};return{init:init};});
